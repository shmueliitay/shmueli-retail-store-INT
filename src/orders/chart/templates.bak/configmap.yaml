apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "retail-store-orders.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "retail-store-orders.name" . }}
data:
  {{- range .Values.config }}
  {{ .name }}: {{ .value | quote }}
  {{- end }}

