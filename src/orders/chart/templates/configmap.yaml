apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "orders.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "orders.name" . }}
data:
  {{- range .Values.config }}
  {{ .name }}: {{ .value | quote }}
  {{- end }}

